# React + Vite

This template provides a minimal setup to get React working in Vite with HMR and some ESLint rules.
 

To run

npm install

npm run dev

NOTE: Make sure the API is running of it will bring up mock data
